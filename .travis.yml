language: python

python:
  - "3.8"

install:
  - pip install Flask==2.2.5
  - pip install Flask-Blueprint==1.3.0
  - pip install flasgger==0.9.5
  - pip install Werkzeug==2.2.3
  - pip install requests

script:
  - python main.py &
  - sleep 5
  - python client.py